apiVersion: v1
kind: Service
metadata:
  name: telecom-soap-backend
  labels:
    app: telecom-soap-backend
spec:
  type: ClusterIP
  selector:
    app: telecom-soap-backend
  ports:
    - port: 8090
      targetPort: 8090
      protocol: TCP
      name: http
